{
  "name": "simple-number-guessing-game",
  "version": "1.0.0",
  "type": "module",
  "description": "A very simple cli game Which asks the user to guess a random number generated by computer, if the guess is closer to the random number, it will display a message saying close enough, if the guess isn't near the number, it will display too far, if correct it will display correct answer!",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "shahabuddin",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.3.0",
    "inquirer": "^9.2.16"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7"
  }
}
